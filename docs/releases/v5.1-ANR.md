# Nova ANR — Phase 5.1 (Production-Grade Adaptive Neural Routing)

**Status:** Released
**Build SHAs:** 66e0a5f, 78c67e0
**Scope:** Production-grade adaptive routing with safety, provenance, and zero-touch ops.

## What changed
- 43 files changed, 3,219 insertions across two clean commits.
- Cross-platform ops: Windows PowerShell 5.1 + Unix-compatible paths/scripts.
- 5 cognitive slots integrated with ANR context: 04 / 07 / 08_lock / 09 / 10.
- End-to-end automation: pilot orchestration, auto-promotion, backups, ledger rotation.

## Safety & ops
- Health gates (critical slots), promotion gates (RSI ≥ 0.85, rollbacks ≤ 0.1/1k live, TRI ≥ 0).
- Kill switch / fast-cap / conservative routing under anomaly.
- Receipts: git SHA + NOVA_* env snapshot + runbook URL.
- DR drill + state backups + rotation (ledger zip archives).

## Provenance & observability
- `nova_build_info{sha="…",component="orchestrator",version="5.1",built_at="…"} 1`
- Decision ledger (NDJSON) with daily report + CSV export.
- Version-agnostic Prometheus content-type tests.

## Go/No-Go
- **Go** when: RSI ≥ 0.85, rollbacks ≤ 0.1/1k, TRI median ≥ 0, no fast-cap breach, critical slots healthy.

## Rollback
- Immediate guardrail: `.\ops\anr-pilot.ps1 -Rollback` (forces R4 route).

*Reflection:* "Intelligence that adapts, safety that scales, operations that honor human judgment." The sun shines on this release. ☀️